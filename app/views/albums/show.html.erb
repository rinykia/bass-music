
<div class="show row">
  <div class="cover col-md-4"><%= @album.cover %></div>
  <div class="col-sm-8">
    <h3 class="album-title"><%= @album.title %></h3>
    <p class="album-artist"><%= @album.artist %></p>
    <div class="track row">
    <%= @trak.each do |t| %>
      <div class="name col-md-8"><%= t.name %></div>
      <div class="minutes col-md-4"><%= t.minutes %>min</div>
    </div>
  </div>
</div>